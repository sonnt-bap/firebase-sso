## KYC System

Ứng dụng Next.js phục vụ thử nghiệm đăng nhập một lần (SSO) với Firebase.

- Cấu hình biến môi trường theo `../.env.example` và ghi vào `./.env.local`.
- Chạy phát triển: `npm run dev` (mặc định http://localhost:3000).
- Thông tin chi tiết nằm trong tài liệu gốc ở `../README.md`.
